import React from 'react';

const Search = () => {
    return (
        <div>
            <h2>Search by Component</h2>
            <input type="text" placeholder="Search by component"/>
            <button> Serch </button>  
        </div>
    );
}

export default Search;